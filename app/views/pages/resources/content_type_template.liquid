---
title: Resource template page
content_type: resources
---
{% extends 'layouts/simple' %}
{% block content %}
<h1>{{ resource.title }}</h1>
<p>{{ resource.description }}</p>

    <a href ="{{ resource.document.url }}"><i><u>{{ resource.document_name }}</u></i></a> {{ resource.resource_date | localized_date: '%d/%m/%Y' }}</p>


{% endblock %}